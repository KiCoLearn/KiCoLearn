<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kids.api.mapper.money">
	<select id="totalMoney" parameterType="int" resultType="int">
		select total_money from kids where kid_id = #{kidId}
	</select>
	
	<insert id="deposit" parameterType="money">
		update kids set total_money = total_money + #{calculation} where kid_id = #{kidId}	
	</insert>
	
	<delete id="withdraw" parameterType="money">
		update kids set total_money = total_money - #{calculation} where kid_id = #{kidId}
	</delete>
	
</mapper>
